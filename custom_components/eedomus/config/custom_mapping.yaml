# Custom device mapping configuration in new dict format
# This format supports dynamic entity properties and advanced rules

# Metadata section (will be loaded by YAML parser)
metadata:
  version: "1.1.0"
  last_modified: "2026-02-24"
  changes:
    - "Initial custom mapping structure"
    - "Ready for custom device overrides"

# Custom advanced rules (empty by default)
custom_rules: []

# Custom usage ID mappings (old custom_device_mappings converted to new format)
custom_usage_id_mappings:
  "1061604":
    ha_entity: "sensor"
    ha_subtype: "usage"
    justification: "Espace libre Box - Specific mapping"
    device_info:
      identifiers:
        - - eedomus
          - eedomus_box_main
      name: "Box eedomus"
      manufacturer: "Eedomus"
      model: "Eedomus Box"
      sw_version: "Unknown"
  
  "1061606":
    ha_entity: "sensor"
    ha_subtype: "usage"
    justification: "Messages Box - Specific mapping"
    device_info:
      identifiers:
        - - eedomus
          - eedomus_box_main
      name: "Box eedomus"
      manufacturer: "Eedomus"
      model: "Eedomus Box"
      sw_version: "Unknown"

# Custom dynamic entity properties (empty by default)
custom_dynamic_entity_properties: {}

# Custom specific device dynamic overrides (empty by default)
custom_specific_device_dynamic_overrides: {}

# Custom temperature setpoint to sensor mappings
# Maps climate entities (usage_id 15) to their corresponding temperature sensors
# This is specific to your personal installation and should not be in the main device_mapping.yaml
temperature_setpoint_mappings:
  '1252440': '1235480'  # Consigne Salle de bain → Sonde température Salle de bain
  '1252441': '1090996'  # Consigne Salon → Sonde température Salon
  '1252442': '3387333'  # Consigne Chambre enfant → Sonde température Chambre enfant
  # Add more mappings here as needed for your specific setup

# Custom name patterns (empty by default)
custom_name_patterns: []
